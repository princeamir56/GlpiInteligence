# Layer 4 — API backend, isolated dependency set.
#
# Kept SEPARATE from the repo-root requirements.txt (which pins SQLAlchemy 1.4 for
# Airflow 2.9). The API needs SQLAlchemy 2.x async + asyncpg, which conflicts with
# Airflow's pin — so the API runs in its own image (Dockerfile.api) with these deps.
fastapi>=0.111
uvicorn[standard]>=0.30
gunicorn>=22.0
sqlalchemy>=2.0
asyncpg>=0.29
redis>=5.0
pydantic>=2.7
pydantic-settings>=2.2
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
slowapi>=0.1.9
prometheus-fastapi-instrumentator>=7.0

# Tests
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
